{
  "name": "fadian",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230404.0",
    "typescript": "^5.0.4",
    "wrangler": "2.15.1"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "view": "bash -c 'echo $npm_package_version'",
    "git:tag": "bash -c 'git tag -a v$npm_package_version -m \"version $npm_package_version\""
  }
}
